# Raspberry Pi Requirements for YOLO Inference
# Install: pip install -r requirements.txt

# Core inference runtime
onnxruntime>=1.17.0        # ONNX model inference (CPU-optimized for ARM)
opencv-python-headless>=4.8.0  # Image I/O + preprocessing (headless = no GUI deps)
numpy>=1.24.0              # Array operations

# Ultralytics (optional — only needed for .pt format inference)
# ultralytics>=8.3.0       # Uncomment if using PyTorch .pt model directly
